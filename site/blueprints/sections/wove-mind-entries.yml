type: pages
parent: kirby.page("wove-mind")
size: small
info: "{{ page.format }}"
# by {{ page.author.name }} on {{ page.date.toDate('d M Y') }}"
layout: cardlets
sortBy: date desc
template: wove-mind-entry
empty: No entries yet
image: 
  query: page.woveMindImages.toFile
  cover: true
  ratio: 2/3
